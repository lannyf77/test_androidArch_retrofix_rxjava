<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="DayTheme" parent="AppTheme.NoActionBar">
        <item name="descriptionColor">@color/day_description</item>
        <item name="commentsColor">@color/day_comments</item>
        <item name="timeColor">@color/day_time</item>
    </style>

    <style name="NightTheme" parent="AppTheme.NoActionBar">
        <item name="descriptionColor">@color/night_description</item>
        <item name="commentsColor">@color/night_comments</item>
        <item name="timeColor">@color/night_time</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="myDialogeTheme" parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:background">#00eee888</item>
        <item name="colorAccent">#ff3367d6</item>
    </style>

</resources>
